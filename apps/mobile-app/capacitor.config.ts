import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.krysinfo.famillink.mobile',
  appName: 'Famillink Mobile',
  webDir: 'dist/mobile-app'
};

export default config;
